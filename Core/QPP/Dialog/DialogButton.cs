using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace QPP.Dialog
{
    public enum DialogButton
    {
        // 摘要:
        //     訊息方塊會顯示 [確定] 按鈕。
        OK = 0,
        //
        // 摘要:
        //     訊息方塊會顯示 [確定] 和 [取消] 按鈕。
        OKCancel = 1,
        //
        // 摘要:
        //     訊息方塊會顯示 [是]、[否] 和 [取消] 按鈕。
        YesNoCancel = 3,
        //
        // 摘要:
        //     訊息方塊會顯示 [是] 和 [否] 按鈕。
        YesNo = 4,
    }
}
